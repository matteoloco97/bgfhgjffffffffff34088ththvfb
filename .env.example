# QuantumDev GPU Setup - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real secrets to version control

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# CPU Server Configuration
# The hostname or IP address of the CPU server where the backend runs
CPU_HOST=cpu-backend.example.com

# Backend API Configuration
# The full URL to the backend API endpoint for GPU registration
BACKEND_API=http://api-server.example.com:8081/update_gpu

# ============================================================================
# OPTIONAL VARIABLES (but recommended for full functionality)
# ============================================================================

# SSH Tunnel Configuration
# SSH private key for establishing secure tunnel to CPU server
# Use the full key including BEGIN/END markers, or path to key file
# If not provided, GPU endpoint will be directly accessible
GPU_SSH_PRIVATE_KEY=

# CPU server SSH user (default: gpu-tunnel)
CPU_USER=gpu-tunnel

# CPU server SSH port (default: 22)
CPU_PORT=22

# Remote tunnel port on CPU server (default: 9001)
CPU_TUNNEL_PORT=9001

# Backend Registration
# Shared secret for HMAC authentication with backend API
# If not provided, backend registration will be skipped
SHARED_SECRET=

# ============================================================================
# EXAMPLE VALUES (for reference only - replace with actual values)
# ============================================================================
# CPU_HOST=84.247.166.247
# BACKEND_API=http://84.247.166.247:8081/update_gpu
# GPU_SSH_PRIVATE_KEY=-----BEGIN OPENSSH PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END OPENSSH PRIVATE KEY-----
# SHARED_SECRET=your_64_character_hex_secret_here
